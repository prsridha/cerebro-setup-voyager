apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.cluster.username }}-etlworkersvc
  namespace: {{ .Values.cluster.namespace }}
  labels:
    app: cerebro-worker-svc
    user: {{ .Values.cluster.username }}
spec:
  selector:
    app: cerebro-etl-worker
    user: {{ .Values.cluster.username }}
  clusterIP: None